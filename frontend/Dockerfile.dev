# Development Dockerfile for Frontend
FROM node:22-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install all dependencies (including dev dependencies)
RUN npm install

# Copy source code (node_modules already exists from RUN npm install)
# Host node_modules will be overridden by anonymous volume in docker-compose
COPY . .

# Expose Vite dev server port
EXPOSE 5173

# Run development server with host 0.0.0.0 to allow external connections
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]

